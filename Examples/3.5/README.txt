Eralp Demir
March 3rd, 2024

This example contains the input parameters sections 3.5 on backstress.


The userinputs.f file needs to be modified for backstress:
- the A-F backstress model is turned on by setting "backstressmodel = 1"
- GND-based backstress model is turned on by setting "backstressmodel = 2"

