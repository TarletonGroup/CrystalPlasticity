Example input file
Each folder contains the fortran code with the parameters set as needed
Copy input file in folder with the entire code and parameters set properly

Run
abaqus job=Job-name cpus=8 gpus=1 user=umat.for

gpus=1 if gpu is present

set proper number of cpus

